import { AccountspayentryModel } from './accountspayentry.model';

describe('AccountspayentryModel', () => {
  it('should create an instance', () => {
    expect(new AccountspayentryModel()).toBeTruthy();
  });
});
